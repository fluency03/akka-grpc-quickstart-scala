

// Generated by Akka gRPC. DO NOT EDIT.
package com.example.helloworld

import scala.concurrent.{ ExecutionContext, Future }

import akka.grpc.scaladsl.{ GrpcExceptionHandler, GrpcMarshalling, ScalapbProtobufSerializer }
import akka.grpc.Codecs

import akka.http.scaladsl.model.{ HttpRequest, HttpResponse, StatusCodes }
import akka.http.scaladsl.model.Uri.Path
import akka.http.scaladsl.model.Uri.Path.Segment

import akka.stream.Materializer

object GreeterServiceHandler {
  private val notFound = Future.successful(HttpResponse(StatusCodes.NotFound))

  /**
   * Creates a `HttpRequest` to `HttpResponse` handler that can be used in for example `Http().bindAndHandleAsync`
   * for the generated partial function handler and ends with `StatusCodes.NotFound` if the request is not matching.
   *
   * Use `akka.grpc.scaladsl.ServiceHandler.concatOrNotFound` with `GreeterServiceHandler.partial` when combining
   * several services.
   */
  def apply(implementation: GreeterService)(implicit mat: Materializer): HttpRequest => Future[HttpResponse] =
    partial(implementation).orElse { case _ => notFound }

  /**
   * Creates a `HttpRequest` to `HttpResponse` handler that can be used in for example `Http().bindAndHandleAsync`
   * for the generated partial function handler and ends with `StatusCodes.NotFound` if the request is not matching.
   *
   * Use `akka.grpc.scaladsl.ServiceHandler.concatOrNotFound` with `GreeterServiceHandler.partial` when combining
   * several services.
   *
   * Registering a gRPC service under a custom prefix is not widely supported and strongly discouraged by the specification.
   */
  def apply(implementation: GreeterService, prefix: String)(implicit mat: Materializer): HttpRequest => Future[HttpResponse] =
    partial(implementation, prefix).orElse { case _ => notFound }

  /**
   * Creates a partial `HttpRequest` to `HttpResponse` handler that can be combined with handlers of other
   * services with `akka.grpc.scaladsl.ServiceHandler.concatOrNotFound` and then used in for example
   * `Http().bindAndHandleAsync`.
   *
   * Use `GreeterServiceHandler.apply` if the server is only handling one service.
   */
  def partial(implementation: GreeterService)(implicit mat: Materializer): PartialFunction[HttpRequest, Future[HttpResponse]] =
    partial(implementation, GreeterService.name)

  /**
   * Creates a partial `HttpRequest` to `HttpResponse` handler that can be combined with handlers of other
   * services with `akka.grpc.scaladsl.ServiceHandler.concatOrNotFound` and then used in for example
   * `Http().bindAndHandleAsync`.
   *
   * Use `GreeterServiceHandler.apply` if the server is only handling one service.
   *
   * Registering a gRPC service under a custom prefix is not widely supported and strongly discouraged by the specification.
   */
  def partial(implementation: GreeterService, prefix: String)(implicit mat: Materializer): PartialFunction[HttpRequest, Future[HttpResponse]] = {
    implicit val ec: ExecutionContext = mat.executionContext
    import GreeterService.Serializers._

    def handle(request: HttpRequest, method: String): Future[HttpResponse] = method match {
      
      case "SayHello" =>
        val responseCodec = Codecs.negotiate(request)
        GrpcMarshalling.unmarshal(request)(HelloRequestSerializer, mat)
          .flatMap(implementation.sayHello(_))
          .map(e => GrpcMarshalling.marshal(e)(HelloReplySerializer, mat, responseCodec))
      
      case "SayHelloToAll" =>
        val responseCodec = Codecs.negotiate(request)
        GrpcMarshalling.unmarshalStream(request)(HelloRequestSerializer, mat)
          .map(implementation.sayHelloToAll(_))
          .map(e => GrpcMarshalling.marshalStream(e)(HelloReplySerializer, mat, responseCodec))
      
      case m => Future.failed(new NotImplementedError(s"Not implemented: $m"))
    }

    Function.unlift((req: HttpRequest) => req.uri.path match {
      case Path.Slash(Segment(`prefix`, Path.Slash(Segment(method, Path.Empty)))) ⇒
        Some(handle(req, method).recoverWith(GrpcExceptionHandler.default))
      case _ =>
        None
    })
  }
}
